<div class="row justify-content-center mt-3">
  <div class="col col-md-6"><h1><%= t('.header') %></h1></div>
  <div class="col-12">
    <div class="row">
      <div class="col-3">
        <%= link_to t('.new_badge'), new_admin_badge_path %>
      </div>
    <%= render 'shared/errors', resource: @badge %>  
  </div>

  <table class="table table-hover">
      <thead class="table align-middle">
      <tr>
        <th scope="col"><%= t('.badge') %></th>
        <th scope="col"><%= t('.title') %></th>
        <th scope="col"><%= t('.rule') %></th>
        <th scope="col", colspan="3", class="text-center"><%= t('tests.index.actions') %></th>
      </tr>
    </thead>

    <tbody>
        <%= render @badges %>
    </tbody>
  </table>
  <div class="col-3">
    <%= link_to t('admin.actions.back'), admin_tests_path %></p>
  </div>
</div>

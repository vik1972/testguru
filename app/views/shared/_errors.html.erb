<% if resource.errors.present? %>
  <div>
  	<h2><%= t('.header', count: resource.errors.count) %> detected:</h2>
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
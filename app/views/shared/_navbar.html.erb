<nav class="navbar navbar-light bg-info rounded">
  <div class="container">
      <%= link_to t('.header'), root_path, class: 'navbar-brand' %>

    <span class="navbar-text">
      <% if user_signed_in? %>

        <% if  current_user.admin? %>
          <%= link_to t('layouts.admin.panel'), admin_tests_path, class: "btn btn-outline-info " %>
        <% end %>

        <%= t('.welcome') %><b><%= current_user.email %></b> 
        <%= link_to t('.logout'), destroy_user_session_path, method: :delete, class: "btn btn-outline-light text-light" %>
      <% else %>
        <%= link_to t('.sign_up'), new_user_registration_path, class: "btn btn-outline-light text-light" %>
        <%= link_to t('.sign_in'), new_user_session_path, class: "btn btn-outline-light text-light" %>
      <% end %>
    </span>
  </div>
</nav>

